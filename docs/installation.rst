============
Installation
============

At the command line::

    $ easy_install django-webex

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-webex
    $ pip install django-webex
